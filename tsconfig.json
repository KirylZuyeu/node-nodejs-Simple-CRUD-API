{
  "compilerOptions": {
    /* Основные настройки */
    "target": "ES2020",                                /* Указывает целевую версию ECMAScript. */
    "module": "CommonJS",                              /* Указывает систему модулей для генерации кода (Node.js по умолчанию). */
    "rootDir": "./src",                                /* Корневой каталог исходных файлов. */
    "outDir": "./dist",                                /* Каталог, куда будут помещены скомпилированные JS-файлы. */

    /* Модули */
    "moduleResolution": "node",                        /* Указывает, как TS будет искать модули. */
    "esModuleInterop": true,                           /* Позволяет импортировать CommonJS модули как ES-модули. */

    /* Type Checking */
    "strict": true,                                    /* Включает все строгие проверки типов. */
    "skipLibCheck": true,                              /* Пропускает проверку деклараций библиотек. */
    "forceConsistentCasingInFileNames": true,          /* Гарантирует, что имена файлов правильно написаны. */
    "resolveJsonModule": true,                         /* Позволяет импортировать .json файлы. */

    /* Emit */
    "sourceMap": true,                                 /* Генерировать .map файлы для отладки. */
    "removeComments": true,                            /* Удалять комментарии при компиляции. */

    /* Другие */
    "lib": ["es2020"],                                 /* Список библиотек, включенных в компиляцию (например, 'dom' или 'es2020'). */
  },
  "include": ["src/**/*", "jest.setup.js"],                          /* Файлы, которые должны быть включены в компиляцию. */
}
